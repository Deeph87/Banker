{%  extends 'base.html.twig' %}

{% block title %}Article {{ article.id }}{% endblock %}

{% block body %}

    <h3>Article {{ article.id }}</h3>

    <h4>- {{ article.title }} </h4>
    <p> {{ article.content|nl2br }} </p>

    <a href="{{ path("app_article_index") }}" class="waves-effect waves-light btn">index</a>
    <a href="{{ path("app_article_edit", {"id": article.id}) }}" class="waves-effect waves-light btn">edit</a>

{% endblock %}